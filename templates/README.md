# Templates

Starter files for new GhCLI graphs.

Use these when creating a new definition from scratch:

- `gh_python_node.py`: minimal Rhino 8 CPython 3 node shape
- `graph-apply-basic.json`: minimal `graph.apply` payload

Templates are not expected to run unchanged. Runnable sample definitions belong in `samples/`.
